sample:
  description: >-
    Example Edge Impulse standalone inferencing application for Zephyr.
  name: example-standalone-inferencing-zephyr-module

common:
  tags: introduction
  harness: console
  harness_config:
    type: one_line
    regex:
      - "run_classifier returned: 0"